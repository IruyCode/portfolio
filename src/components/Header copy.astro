---
const { lang = 'pt', t = {} } = Astro.props;
---

<!-- Menu de Navegação -->
<header
    class="fixed inset-x-0 top-0 z-30 mx-auto w-full max-w-screen-md border border-white/10 bg-black/100 py-3 shadow backdrop-blur-md md:top-6 md:rounded-3xl lg:max-w-screen-lg">
    <div class="px-4">
        <div class="flex items-center justify-between">
            <!-- Logo -->
            <div class="flex shrink-0">
                <a class="flex items-center gap-2" href="/">
                    <img class="h-9 w-auto drop-shadow-md" src="/imgs/logo/log.png" alt="Logo" />
                    <span class="sr-only">IruyCode</span>
                </a>
            </div>

            <!-- Menu -->
            <div class="hidden md:flex md:items-center md:justify-center md:gap-5">
                <a class="text-sm font-medium text-gray-200 hover:text-cyan-400 transition"
                    href="#">{t.menu.home}</a>
                <a class="text-sm font-medium text-gray-200 hover:text-cyan-400 transition"
                    href="#">{t.menu.about}</a>
                <a class="text-sm font-medium text-gray-200 hover:text-cyan-400 transition"
                    href="#">{t.menu.works}</a>
                <a class="text-sm font-medium text-gray-200 hover:text-cyan-400 transition"
                    href="#">{t.menu.projects}</a>
                <a class="text-sm font-medium text-gray-200 hover:text-cyan-400 transition"
                    href="#">{t.menu.contacts}</a>
            </div>

            <!-- Idioma e Tema -->
            <div class="flex items-center gap-3">
                <!-- Idioma -->
                <div class="relative">
                    <button id="langToggle"
                        class="flex items-center text-sm text-white hover:text-cyan-400 gap-1 focus:outline-none">
                        <span id="langFlag">
                            {lang === "en" ? "🇺🇸" : lang === "es" ? "🇪🇸" : "🇧🇷"}
                        </span>
                        <span class="hidden sm:inline">
                            {lang.toUpperCase()}
                        </span>
                        <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7">
                            </path>
                        </svg>
                    </button>

                    <!-- Dropdown -->
                    <div id="langMenu"
                        class="absolute right-0 mt-2 w-28 bg-white rounded shadow-lg py-1 hidden z-50 text-sm">
                        <a href="/" class="flex items-center gap-2 px-4 py-2 text-gray-700 hover:bg-gray-100">🇧🇷
                            PT</a>
                        <a href="/en/" class="flex items-center gap-2 px-4 py-2 text-gray-700 hover:bg-gray-100">🇺🇸
                            EN</a>
                        <a href="/es/" class="flex items-center gap-2 px-4 py-2 text-gray-700 hover:bg-gray-100">🇪🇸
                            ES</a>
                    </div>
                </div>

                <!-- Tema claro/escuro -->
                <button id="theme-toggle" class="text-gray-200 hover:text-cyan-400 focus:outline-none"
                    aria-label="Alternar tema">
                    <svg id="icon-sun" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <circle cx="12" cy="12" r="5" stroke-width="2"></circle>
                        <path stroke-width="2"
                            d="M12 1v2m0 18v2m11-11h-2M3 12H1m16.95 6.95l-1.41-1.41M6.46 6.46L5.05 5.05m13.9 0l-1.41 1.41M6.46 17.54l-1.41 1.41">
                        </path>
                    </svg>
                    <svg id="icon-moon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-width="2" d="M21 12.79A9 9 0 1111.21 3a7 7 0 109.79 9.79z"></path>
                    </svg>
                </button>
            </div>
        </div>
    </div>
</header>
